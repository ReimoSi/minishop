# Ära tee skeemi Hibernate'iga
spring.jpa.hibernate.ddl-auto=none
spring.jpa.properties.hibernate.hbm2ddl.auto=none

# Testides luba schema.sql + data.sql (sinu skeem ja seed)
spring.sql.init.mode=always
spring.sql.init.schema-locations=classpath:/schema.sql
spring.sql.init.data-locations=classpath:/data.sql
spring.sql.init.continue-on-error=true

# Keela Liquibase testides (meil pole siin migrationeid vaja)
spring.liquibase.enabled=false

# (valikuline) kui kasutad oma Postgrest testides:
# spring.datasource.url=jdbc:postgresql://localhost:5432/minishop
# spring.datasource.username=postgres
# spring.datasource.password=postgres
